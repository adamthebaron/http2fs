#include <u.h>
#include <libc.h>
#include <stdio.h>

#include "http2fs.h"
#include "hpack.h"

void
inithpackdynamictable() {
	//for(int i = 0; i < 1024; i++)
	//	hpackdynamictable[i] = malloc(sizeof(HHeader));
	//index = 0;
	//offset = 0;
}